import { ActionTypes } from "../Types";

const initialState = {
    cart : [],
    isLoading: false,
}


export const CartReducer = (state= initialState, action) =>{

    switch(action.type){
        
    }
}